# Wishlist service
# Wishlist for users
version: v0.0.4
name: wishlist
ingressAdapters:
# Edit wishlist
- stateless:
    action: write
    variant: rest
    route: update
  steps:
  - egressAdapter:
      stateful:
        name: cart-db
        variant: mongo
        action: write
        size: small
  - egressAdapter:
      stateless:
        variant: rest
        service: recommendation
        action: write
        route: update
  - egressAdapter:
      stateless:
        variant: rest
        service: user-preference
        action: write
        route: update

# get wishilist content
- stateless:
    action: read
    variant: rest
    route: get
  steps:
  - egressAdapter:
      stateful:
        name: cart-db
        variant: mongo
        action: read
        size: small
